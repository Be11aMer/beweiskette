{
  "name": "beweiskette",
  "version": "1.0.0",
  "description": "Digital evidence chain of custody — zero-runtime-dependency web app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.js\"",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "license": "MIT",
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
